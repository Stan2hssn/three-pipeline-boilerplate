/**
 * Input - Manager (events later). RAF calls input.update(time, dt) each frame.
 */
export default class InputBase {
  constructor() {
    //
  }

  update(_time: number, _dt: number): void {
    // TODO: events
  }
}